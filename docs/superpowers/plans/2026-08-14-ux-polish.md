# FlowKit UX 打磨实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 完成 10 项 UX 增强（主题/弹窗编辑/震动增强/日志修复/图标/模板/复制/直达/导入导出/短信记录）。

**Architecture:** 主题层（Token + Provider）→ 数据层（enabled 字段、SmsRecord store、日志 append 修复）→ 原生层（VibrationModule amplitude、图标）→ UI 层（Modal 化编辑、日志 Tab、模板、复制、导入导出、通知直达）。

**Tech Stack:** RN 0.86, zustand, AsyncStorage, Notifee, Kotlin

**Spec:** `docs/superpowers/specs/2026-08-14-ux-polish-design.md`
**分支:** `feat/ux-polish`

---

### Task 1: 主题系统（Token + Provider）

**Files:**
- Create: `src/theme/colors.ts`
- Create: `src/theme/ThemeContext.tsx`
- Create: `src/theme/index.ts`
- Modify: `src/app/App.tsx`
- Modify: `src/app/screens/HomeScreen.tsx`（切换按钮）

- [ ] **Step 1: 颜色 Token**

Create `src/theme/colors.ts`:

```typescript
export type ThemeMode = 'light' | 'dark' | 'system';

export interface ThemeColors {
  background: string;
  surface: string;
  surfaceAlt: string;
  text: string;
  textSecondary: string;
  textMuted: string;
  primary: string;
  border: string;
  danger: string;
  success: string;
  warning: string;
  warningBg: string;
  headerBg: string;
}

export const lightColors: ThemeColors = {
  background: '#f5f5f5',
  surface: '#ffffff',
  surfaceAlt: '#f0f0f0',
  text: '#1a1a1a',
  textSecondary: '#666666',
  textMuted: '#999999',
  primary: '#4a90d9',
  border: '#e0e0e0',
  danger: '#d9534f',
  success: '#5cb85c',
  warning: '#b26a00',
  warningBg: '#fff8e1',
  headerBg: '#ffffff',
};

export const darkColors: ThemeColors = {
  background: '#121212',
  surface: '#1e1e1e',
  surfaceAlt: '#2a2a2a',
  text: '#f0f0f0',
  textSecondary: '#aaaaaa',
  textMuted: '#777777',
  primary: '#5b9bd5',
  border: '#333333',
  danger: '#e0706c',
  success: '#6cc06c',
  warning: '#d9a03f',
  warningBg: '#3a3120',
  headerBg: '#1e1e1e',
};
```

- [ ] **Step 2: ThemeProvider**

Create `src/theme/ThemeContext.tsx`:

```typescript
import React, { createContext, useContext, useEffect, useMemo, useState } from 'react';
import { Appearance, useColorScheme } from 'react-native';
import { darkColors, lightColors, type ThemeColors, type ThemeMode } from './colors';

interface ThemeContextValue {
  mode: ThemeMode;
  colors: ThemeColors;
  isDark: boolean;
  setMode: (m: ThemeMode) => void;
  cycleMode: () => void;
}

const ThemeContext = createContext<ThemeContextValue>({
  mode: 'system',
  colors: lightColors,
  isDark: false,
  setMode: () => {},
  cycleMode: () => {},
});

export function ThemeProvider({ children }: { children: React.ReactNode }) {
  const systemScheme = useColorScheme();
  const [mode, setModeState] = useState<ThemeMode>('system');
  const [manualDark, setManualDark] = useState<boolean | null>(null);

  const isDark = mode === 'system' ? systemScheme === 'dark' : mode === 'dark';

  useEffect(() => {
    // 保持 useColorScheme 订阅活跃（Appearance.setColorScheme 手动模式）
    if (mode === 'light') Appearance.setColorScheme('light');
    else if (mode === 'dark') Appearance.setColorScheme('dark');
    else Appearance.setColorScheme(null);
  }, [mode]);

  const value = useMemo(
    () => ({
      mode,
      colors: isDark ? darkColors : lightColors,
      isDark,
      setMode: setModeState,
      cycleMode: () => {
        setModeState((m) => (m === 'light' ? 'dark' : m === 'dark' ? 'system' : 'light'));
      },
    }),
    [mode, isDark],
  );

  return <ThemeContext.Provider value={value}>{children}</ThemeContext.Provider>;
}

export function useTheme(): ThemeContextValue {
  return useContext(ThemeContext);
}
```

- [ ] **Step 3: 导出**

Create `src/theme/index.ts`:

```typescript
export { ThemeProvider, useTheme } from './ThemeContext';
export type { ThemeMode, ThemeColors } from './colors';
```

- [ ] **Step 4: App.tsx 包 Provider**

Read `src/app/App.tsx` 现有内容，在 `export default function App()` 的 return 最外层包 `<ThemeProvider>`，例如：

```typescript
import { ThemeProvider } from '../theme';
// ... 现有 import
export default function App() {
  // ... 现有逻辑
  return (
    <ThemeProvider>
      {/* 现有 NavigationContainer 等 */}
    </ThemeProvider>
  );
}
```

- [ ] **Step 5: 首页切换按钮**

Read `src/app/screens/HomeScreen.tsx`，在 header 或右上角加主题循环按钮：

```typescript
import { useTheme } from '../../theme';

export default function HomeScreen() {
  const { cycleMode, mode, colors } = useTheme();
  // 在 headerRight 或固定位置加：
  // <TouchableOpacity onPress={cycleMode}>
  //   <Text style={{ color: colors.primary }}>
  //     {mode === 'light' ? '浅色' : mode === 'dark' ? '深色' : '自适应'}
  //   </Text>
  // </TouchableOpacity>
}
```

- [ ] **Step 6: 提交**

```bash
git add src/theme src/app/App.tsx src/app/screens/HomeScreen.tsx
git commit -m "feat: add theme system with light/dark/system modes and provider"
```

---

### Task 2: 全 UI 颜色 Token 化

**Files:**
- Modify: 全部页面/组件（src/app/screens/HomeScreen.tsx、src/modules/trigger/screens/*.tsx、src/modules/trigger/components/*.tsx）

- [ ] **Step 1: 批量替换**

对每个页面/组件：
1. `import { useTheme } from '../../../theme';`（按相对路径调整）
2. 组件内 `const { colors } = useTheme();`
3. 把所有 `StyleSheet.create` 里的硬编码颜色替换为 colors 引用（StyleSheet 不能直接用 hook 值——改为在组件内用 `useMemo` 构建 style 或把样式对象移到组件内）：

对每个文件，将 `const styles = StyleSheet.create({...})` 改为组件内：

```typescript
const { colors } = useTheme();
const styles = useMemo(
  () =>
    StyleSheet.create({
      container: { flex: 1, backgroundColor: colors.background },
      // ... 其余样式，硬编码色值替换为 colors.xxx
    }),
  [colors],
);
```

颜色映射约定：
- `#f5f5f5` → `colors.background`；`#fff` → `colors.surface`
- `#4a90d9` → `colors.primary`；`#1a1a1a`/`#333` → `colors.text`
- `#666` → `colors.textSecondary`；`#999` → `colors.textMuted`
- `#e0e0e0` → `colors.border`；`#d9534f`/红 → `colors.danger`
- `#5cb85c`/绿 → `colors.success`；`#b26a00`/`#fff8e1` → `colors.warning`/`colors.warningBg`

涉及文件（逐一处理，确保 tsc 通过）：
- `src/app/screens/HomeScreen.tsx`
- `src/modules/trigger/screens/RuleListScreen.tsx`
- `src/modules/trigger/screens/RuleEditScreen.tsx`
- `src/modules/trigger/screens/LogScreen.tsx`
- `src/modules/trigger/components/ConditionEditor.tsx`
- `src/modules/trigger/components/ActionEditor.tsx`
- `src/modules/trigger/components/SimulateSmsModal.tsx`

- [ ] **Step 2: tsc + 提交**

Run: `npx tsc --noEmit`（Expected: 0 报错）

```bash
git add -A src/
git commit -m "refactor: tokenize all UI colors with theme system"
```

---

### Task 3: 动作启用开关（enabled 字段）

**Files:**
- Modify: `src/modules/trigger/types/index.ts`
- Modify: `src/modules/trigger/services/ActionExecutor.ts`
- Test: `__tests__/RuleEngine.test.ts`（追加）

- [ ] **Step 1: 类型加 enabled**

In `src/modules/trigger/types/index.ts`，`TriggerAction` 接口加字段：

```typescript
export interface TriggerAction {
  id: string;
  type: 'notify' | 'vibrate' | 'ringtone' | 'pushToWatch';
  params?: Record<string, unknown>;
  /** 动作是否启用（false 时执行阶段跳过） */
  enabled?: boolean;
}
```

- [ ] **Step 2: ActionExecutor 跳过 disabled**

In `src/modules/trigger/services/ActionExecutor.ts`，`execute()` 的动作循环内，取 handler 之前加：

```typescript
      for (const action of match.rule.actions) {
        if (action.enabled === false) {
          actionLogs.push({
            type: action.type,
            success: true,
            error: '已停用',
          });
          continue;
        }
        const handler = actionHandlers.get(action.type);
```

- [ ] **Step 3: 追加单测**

In `__tests__/RuleEngine.test.ts` 追加：

```typescript
describe('disabled action skipping', () => {
  it('skips actions with enabled=false in executor log', async () => {
    // 直接验证 ActionExecutor.execute 跳过逻辑
    const { ActionExecutor } = await import('../src/modules/trigger/services/ActionExecutor');
    const logs = await ActionExecutor.execute(
      [
        {
          rule: {
            id: 'r1',
            name: 'r',
            enabled: true,
            conditions: [],
            actions: [
              { id: 'a1', type: 'notify', params: { title: 't' }, enabled: false },
            ],
          },
          matchedConditions: [],
        } as any,
      ],
      { sender: 's', body: 'b' },
    );
    expect(logs[0].actions[0].success).toBe(true);
    expect(logs[0].actions[0].error).toBe('已停用');
  });
});
```

- [ ] **Step 4: jest + tsc + 提交**

Run: `npx jest && npx tsc --noEmit`（全绿）

```bash
git add src/modules/trigger/types/index.ts src/modules/trigger/services/ActionExecutor.ts __tests__/RuleEngine.test.ts
git commit -m "feat: add action enable switch with skip logic in executor"
```

---

### Task 4: 编辑页卡片弹窗化 + 动作开关 UI + 条件删除隐藏

**Files:**
- Modify: `src/modules/trigger/screens/RuleEditScreen.tsx`
- Modify: `src/modules/trigger/components/ConditionEditor.tsx`
- Modify: `src/modules/trigger/components/ActionEditor.tsx`

- [ ] **Step 1: ConditionEditor 隐藏删除**

In `ConditionEditor.tsx`，删除按钮（content-desc="✕"）渲染条件改为：父组件通过 props 传入 `showDelete`（条件数 > 1 时 true）；组件内 `{showDelete && <TouchableOpacity ...>✕</TouchableOpacity>}`。

接口变更：`interface ConditionEditorProps { condition, onChange, showDelete, onDelete }`

- [ ] **Step 2: ActionEditor 加开关**

In `ActionEditor.tsx`：props 增加 `enabled: boolean` 与 `onToggleEnabled: (v: boolean) => void`；卡片顶部渲染 `Switch value={enabled} onValueChange={onToggleEnabled}`。

- [ ] **Step 3: RuleEditScreen 弹窗化**

In `RuleEditScreen.tsx`：
- 条件列表每项点击 → 打开 `Modal`（透明，居中卡片）内嵌该条件的编辑表单；Modal 底部「完成」按钮关闭
- 动作列表每项点击 → 类似 Modal 编辑该动作（含类型选择 + 参数 + 开关）
- 条件仅 1 条时传 `showDelete={conditions.length > 1}`
- 保存逻辑不变（updateRule/addRule 透传 enabled）

- [ ] **Step 4: tsc + 提交**

Run: `npx tsc --noEmit`

```bash
git add src/modules/trigger/screens/RuleEditScreen.tsx src/modules/trigger/components/ConditionEditor.tsx src/modules/trigger/components/ActionEditor.tsx
git commit -m "feat: modal-based card editing with action switch and delete guard"
```

---

### Task 5: 震动增强（模式/节奏/力度）

**Files:**
- Modify: `src/modules/trigger/types/index.ts`（ACTION_META.vibrate）
- Modify: `src/modules/trigger/services/ActionExecutor.ts`（vibrate handler）
- Modify: `android/app/src/main/java/com/flowkit/FlowKitPackage.kt`
- Create: `android/app/src/main/java/com/flowkit/VibrationModule.kt`

- [ ] **Step 1: ACTION_META 震动参数**

In `src/modules/trigger/types/index.ts`，vibrate meta 改为：

```typescript
  {
    type: 'vibrate',
    label: '震动',
    params: [
      { key: 'mode', label: '模式', placeholder: 'gentle|standard|urgent|custom' },
      { key: 'pattern', label: '节奏(ms,逗号分隔)', placeholder: '如 200,100,200' },
      { key: 'amplitude', label: '力度(1-255)', placeholder: '留空自动', numeric: true },
    ],
  },
```

- [ ] **Step 2: 原生 VibrationModule**

Create `android/app/src/main/java/com/flowkit/VibrationModule.kt`:

```kotlin
package com.flowkit

import android.os.Build
import android.os.VibrationEffect
import android.os.Vibrator
import android.os.VibratorManager
import com.facebook.react.bridge.ReactApplicationContext
import com.facebook.react.bridge.ReactContextBaseJavaModule
import com.facebook.react.bridge.ReactMethod

class VibrationModule(private val reactContext: ReactApplicationContext) :
  ReactContextBaseJavaModule(reactContext) {

  override fun getName(): String = "VibrationModule"

  @ReactMethod
  fun vibrate(pattern: String, amplitude: Int) {
    val vibrator: Vibrator? =
      if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.S) {
        reactContext.getSystemService(VibratorManager::class.java)?.defaultVibrator
      } else {
        @Suppress("DEPRECATION")
        reactContext.getSystemService(android.content.Context.VIBRATOR_SERVICE) as? Vibrator
      } ?: return

    val times = pattern.split(",").mapNotNull { it.trim().toLongOrNull() }.toLongArray()
    if (times.isEmpty()) {
      vibrator.vibrate(VibrationEffect.createOneShot(500, normalizedAmplitude(amplitude)))
      return
    }
    // times 为 [on, off, on, off...]；createWaveform 接受间隔数组
    val waveform = if (times.size % 2 == 0) times else times + longArrayOf(1000)
    val amp = normalizedAmplitude(amplitude)
    val effect: VibrationEffect =
      if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.O) {
        VibrationEffect.createWaveform(waveform, amp)
      } else {
        @Suppress("DEPRECATION")
        android.os.Vibrator.VibrationEffectFactory.createWaveform(waveform, 0)
      }
    vibrator.vibrate(effect)
  }

  private fun normalizedAmplitude(amp: Int): Int =
    if (amp in 1..255) amp else VibrationEffect.DEFAULT_AMPLITUDE
}
```

> 注：`Vibrator.VibrationEffectFactory` 不存在于 API<26，上面 else 分支仅示意——本项目 minSdk 24，用 `@Suppress("DEPRECATION") vibrator.vibrate(waveform, 0)` 兼容（`Vibrator.vibrate(long[], int)` API 1 就有）。若编译报错，把 else 分支改为 `vibrator.vibrate(waveform, -1)`（旧 API 无 amplitude）。

- [ ] **Step 3: 注册原生模块**

In `FlowKitPackage.kt`，createNativeModules 列表追加 `VibrationModule(reactContext)`。

- [ ] **Step 4: ActionExecutor vibrate handler**

In `ActionExecutor.ts`，替换 vibrate handler：

```typescript
    this.registerHandler('vibrate', async (action) => {
      const params = action.params ?? {};
      const mode = String(params.mode ?? 'standard');
      const patternRaw = typeof params.pattern === 'string' ? params.pattern : '';
      const amplitude =
        typeof params.amplitude === 'number' ? params.amplitude : 0;
      // 预置模式 → pattern
      let pattern: string;
      switch (mode) {
        case 'gentle':
          pattern = '100';
          break;
        case 'urgent':
          pattern = '200,80,200,80,300';
          break;
        case 'custom':
          pattern = patternRaw || '200,100,200';
          break;
        case 'standard':
        default:
          pattern = '300';
          break;
      }
      const nativeVibrate = NativeModules.VibrationModule;
      if (nativeVibrate) {
        nativeVibrate.vibrate(pattern, amplitude);
        return { success: true };
      }
      // 降级：RN 内置 Vibration
      const Vibration = require('react-native').Vibration;
      const parts = pattern.split(',').map((p) => Number(p) || 0);
      Vibration.vibrate(parts.length > 1 ? parts : parts[0], false);
      return { success: true };
    });
```

（`NativeModules` 已在文件顶部 import。）

- [ ] **Step 5: 编译 + tsc + 提交**

Run: `cd android && ./gradlew :app:compileDebugKotlin` 和 `npx tsc --noEmit`

```bash
git add src/modules/trigger/types/index.ts src/modules/trigger/services/ActionExecutor.ts android/app/src/main/java/com/flowkit/VibrationModule.kt android/app/src/main/java/com/flowkit/FlowKitPackage.kt
git commit -m "feat: enhance vibration with mode/pattern/amplitude via native module"
```

---

### Task 6: 日志全量记录修复（覆盖丢失）

**Files:**
- Modify: `src/modules/trigger/store/index.ts`
- Modify: `src/modules/trigger/services/RuleStorage.ts`

- [ ] **Step 1: RuleStorage.saveLogs 改 append-merge**

In `RuleStorage.ts`，`saveLogs` 改为从存储读现有再合并：

```typescript
  async saveLogs<T>(logs: T[]): Promise<void> {
    // 从存储读现有日志（避免进程内 state 为空时覆盖历史）
    const existing = await this.loadLogs<T>();
    const merged = [...existing, ...logs];
    const trimmed = merged.slice(-2000); // 上限提到 2000
    await AsyncStorage.setItem(LOGS_KEY, JSON.stringify(trimmed));
  },
```

- [ ] **Step 2: store.processSms 先加载现有日志**

In `store/index.ts`，`processSms` 开头（`const { rules, logs } = get()` 之后、执行前）加：

```typescript
    if (logs.length === 0) {
      await get().loadLogs();
    }
```

并把 `const { rules, logs } = get();` 移到 loadLogs 之后重新取值：

```typescript
  async processSms(sender, body) {
    const { rules } = get();
    if (get().logs.length === 0) {
      await get().loadLogs();
    }
    const { logs } = get();
    const matches = RuleEngine.compare({ sender, body }, rules);
    ...
  }
```

- [ ] **Step 3: 提交**

```bash
git add src/modules/trigger/store/index.ts src/modules/trigger/services/RuleStorage.ts
git commit -m "fix: merge logs from storage to prevent overwriting history"
```

---

### Task 7: 短信记录 store（SmsRecord）

**Files:**
- Create: `src/modules/trigger/services/SmsLogStorage.ts`
- Create: `src/modules/trigger/services/SmsLogStore.ts`
- Modify: `src/modules/trigger/store/index.ts`（processSms 写入）

- [ ] **Step 1: SmsLogStorage**

Create `src/modules/trigger/services/SmsLogStorage.ts`:

```typescript
import AsyncStorage from '@react-native-async-storage/async-storage';
import type { SmsRecord } from '../types';

const SMS_LOG_KEY = '@flowkit:trigger:sms-log';

export const SmsLogStorage = {
  async load(): Promise<SmsRecord[]> {
    const raw = await AsyncStorage.getItem(SMS_LOG_KEY);
    if (!raw) return [];
    try {
      return JSON.parse(raw) as SmsRecord[];
    } catch {
      return [];
    }
  },
  async save(records: SmsRecord[]): Promise<void> {
    const trimmed = records.slice(-500);
    await AsyncStorage.setItem(SMS_LOG_KEY, JSON.stringify(trimmed));
  },
};
```

- [ ] **Step 2: SmsRecord 类型**

In `src/modules/trigger/types/index.ts` 追加：

```typescript
/** 监听到的短信记录（无论是否命中规则） */
export interface SmsRecord {
  id: string;
  sender: string;
  body: string;
  receivedAt: number;
  /** 命中的规则名列表（未命中为空数组） */
  matchedRuleNames: string[];
}
```

- [ ] **Step 3: SmsLogStore**

Create `src/modules/trigger/services/SmsLogStore.ts`:

```typescript
import { create } from 'zustand';
import type { SmsRecord } from '../types';
import { SmsLogStorage } from './SmsLogStorage';

interface SmsLogState {
  records: SmsRecord[];
  load: () => Promise<void>;
  add: (record: SmsRecord) => Promise<void>;
}

export const useSmsLogStore = create<SmsLogState>((set, get) => ({
  records: [],
  async load() {
    const records = await SmsLogStorage.load();
    set({ records });
  },
  async add(record) {
    const records = [...get().records, record];
    await SmsLogStorage.save(records);
    set({ records });
  },
}));
```

- [ ] **Step 4: processSms 写入**

In `store/index.ts` 的 `processSms`，`RuleEngine.compare` 之后：

```typescript
    const matchedRuleNames = matches.map((m) => m.rule.name);
    await useSmsLogStore.getState().add({
      id: generateId(),
      sender,
      body,
      receivedAt: Date.now(),
      matchedRuleNames,
    });
```

（import `useSmsLogStore` from '../services/SmsLogStore'）

- [ ] **Step 5: tsc + 提交**

```bash
git add src/modules/trigger/services/SmsLogStorage.ts src/modules/trigger/services/SmsLogStore.ts src/modules/trigger/types/index.ts src/modules/trigger/store/index.ts
git commit -m "feat: add sms received log store with match status"
```

---

### Task 8: 日志页 Tab（触发日志 / 短信记录）

**Files:**
- Modify: `src/modules/trigger/screens/LogScreen.tsx`

- [ ] **Step 1: Tab 切换**

In `LogScreen.tsx`：
1. `import { useSmsLogStore } from '../services/SmsLogStore';` + `const { records, load } = useSmsLogStore();`
2. `useEffect` 同时 `void load();`
3. 顶部两个 Tab 按钮（触发日志 / 短信记录），`useState<'logs' | 'sms'>('logs')` 控制
4. 短信记录列表：每条显示发件人、正文（numberOfLines=2）、时间、命中状态标签（`matchedRuleNames.length ? `命中: ${names.join(', ')}` : '未命中'`，命中绿/未命中灰）

- [ ] **Step 2: tsc + 提交**

```bash
git add src/modules/trigger/screens/LogScreen.tsx
git commit -m "feat: add sms log tab with match status to log screen"
```

---

### Task 9: 模拟短信快捷模板

**Files:**
- Modify: `src/modules/trigger/components/SimulateSmsModal.tsx`

- [ ] **Step 1: 模板 chips**

In `SimulateSmsModal.tsx`，正文输入框上方加模板区：

```typescript
const TEMPLATES = ['未按规定停放', '您的验证码是 123456', '余额不足', '到账提醒'];

{/* 模板快捷填入 */}
<View style={{ flexDirection: 'row', flexWrap: 'wrap', gap: 8 }}>
  {TEMPLATES.map((t) => (
    <TouchableOpacity
      key={t}
      onPress={() => setBody(t)}
      style={{ backgroundColor: colors.surfaceAlt, borderRadius: 16, paddingHorizontal: 12, paddingVertical: 6 }}>
      <Text style={{ color: colors.textSecondary, fontSize: 12 }}>{t}</Text>
    </TouchableOpacity>
  ))}
</View>
```

（需 `const { colors } = useTheme();`）

- [ ] **Step 2: tsc + 提交**

```bash
git add src/modules/trigger/components/SimulateSmsModal.tsx
git commit -m "feat: add quick sms templates to simulate modal"
```

---

### Task 10: 规则复制

**Files:**
- Modify: `src/modules/trigger/store/index.ts`
- Modify: `src/modules/trigger/screens/RuleListScreen.tsx`

- [ ] **Step 1: store 加 duplicateRule**

In `store/index.ts`，interface 与实现追加：

```typescript
  duplicateRule: (id: string) => Promise<void>;
```

```typescript
  async duplicateRule(id) {
    const rule = get().rules.find((r) => r.id === id);
    if (!rule) return;
    const copy: TriggerRule = {
      ...rule,
      id: generateId(),
      name: `${rule.name} 副本`,
      createdAt: Date.now(),
    };
    const rules = [...get().rules, copy];
    await RuleStorage.saveRules(rules);
    set({ rules });
  },
```

- [ ] **Step 2: 列表长按复制**

In `RuleListScreen.tsx`，规则卡片 `TouchableOpacity` 加 `onLongPress`：

```typescript
      onLongPress={() => {
        Alert.alert('复制规则', `复制「${item.name}」？`, [
          { text: '取消', style: 'cancel' },
          { text: '复制', onPress: () => void duplicateRule(item.id) },
        ]);
      }}
```

（解构 `duplicateRule`，`Alert` 从 react-native import）

- [ ] **Step 3: tsc + 提交**

```bash
git add src/modules/trigger/store/index.ts src/modules/trigger/screens/RuleListScreen.tsx
git commit -m "feat: duplicate rule on long press"
```

---

### Task 11: 通知直达日志

**Files:**
- Create: `src/modules/trigger/services/NotificationNavigation.ts`
- Modify: `src/modules/trigger/index.ts`

- [ ] **Step 1: 全局导航引用**

Create `src/modules/trigger/services/NotificationNavigation.ts`:

```typescript
import { createNavigationContainerRef } from '@react-navigation/native';

export const navigationRef = createNavigationContainerRef<Record<string, never>>();

export function navigateToLog(): void {
  if (navigationRef.isReady()) {
    navigationRef.navigate('TriggerLog' as never);
  }
}
```

- [ ] **Step 2: App.tsx 绑定 ref**

In `src/app/App.tsx`，`NavigationContainer` 加 `ref={navigationRef}`（import 自 '../modules/trigger/services/NotificationNavigation'）。

- [ ] **Step 3: Notifee 事件处理**

In `src/modules/trigger/index.ts`（registerTriggerModule 内，Android 分支）：

```typescript
import notifee, { EventType } from '@notifee/react-native';
import { navigateToLog } from './services/NotificationNavigation';

// 注册模块时：
notifee.onForegroundEvent(({ type }) => {
  if (type === EventType.PRESS) navigateToLog();
});
```

- [ ] **Step 4: tsc + 提交**

```bash
git add src/modules/trigger/services/NotificationNavigation.ts src/app/App.tsx src/modules/trigger/index.ts
git commit -m "feat: navigate to log screen on notification press"
```

---

### Task 12: 规则导入导出

**Files:**
- Create: `src/modules/trigger/services/RuleExport.ts`
- Modify: `src/modules/trigger/screens/RuleListScreen.tsx`

- [ ] **Step 1: RuleExport 工具**

Create `src/modules/trigger/services/RuleExport.ts`:

```typescript
import { Share } from 'react-native';
import type { TriggerRule } from '../types';

export function serializeRules(rules: TriggerRule[]): string {
  return JSON.stringify(
    { app: 'FlowKit', version: 1, exportedAt: Date.now(), rules },
    null,
    2,
  );
}

export function parseRules(json: string): { rules: TriggerRule[]; error?: string } {
  try {
    const data = JSON.parse(json);
    if (!data || data.app !== 'FlowKit' || !Array.isArray(data.rules)) {
      return { rules: [], error: '不是有效的 FlowKit 规则文件' };
    }
    return { rules: data.rules as TriggerRule[] };
  } catch {
    return { rules: [], error: 'JSON 解析失败' };
  }
}

export async function exportRules(rules: TriggerRule[]): Promise<void> {
  await Share.share({ message: serializeRules(rules) });
}
```

- [ ] **Step 2: UI 入口**

In `RuleListScreen.tsx`，headerRight 增加「导出」按钮；导入用 `Alert.prompt`（iOS only）不可靠——Android 用「导入」按钮 → `Share` 不可输入。方案：列表页 header 加「导出」，**导入**放到「日志」页旁边或列表页长按菜单。**实现：列表页 header 加「导出」「导入」两个文字按钮，导入时弹出 Modal 粘贴 JSON**（新增一个轻量 Modal：TextInput 多行 + 确认导入）。

导入逻辑：

```typescript
const { error, rules: imported } = parseRules(text);
if (error) {
  Alert.alert('导入失败', error);
  return;
}
for (const r of imported) {
  await addRule({ name: r.name, enabled: r.enabled ?? true, conditions: r.conditions, actions: r.actions });
}
Alert.alert('导入成功', `已导入 ${imported.length} 条规则`);
```

- [ ] **Step 3: tsc + 提交**

```bash
git add src/modules/trigger/services/RuleExport.ts src/modules/trigger/screens/RuleListScreen.tsx
git commit -m "feat: export and import rules as JSON"
```

---

### Task 13: 应用图标集成（待主人素材）

**Files:**
- Create/Replace: `android/app/src/main/res/mipmap-*/ic_launcher*.png`
- Create: `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml`（adaptive）

- [ ] **Step 1: 生成密度图**

主人选定源图（如 `/tmp/icon_mint/mint_XX.png`）后，用脚本生成 5 档 mipmap：

```bash
python3 << 'EOF'
from PIL import Image
src = '/tmp/icon_mint/mint_01_bolt_mint_light.png'  # 主人选定文件
sizes = {'mdpi': 48, 'hdpi': 72, 'xhdpi': 96, 'xxhdpi': 144, 'xxxhdpi': 192}
img = Image.open(src).convert('RGBA')
for name, size in sizes.items():
    img.resize((size, size), Image.LANCZOS).save(
        f'android/app/src/main/res/mipmap-{name}/ic_launcher.png')
EOF
```

- [ ] **Step 2: adaptive icon（API 26+）**

Create `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher.xml`:

```xml
<?xml version="1.0" encoding="utf-8"?>
<adaptive-icon xmlns:android="http://schemas.android.com/apk/res/android">
  <background android:drawable="@color/ic_launcher_background"/>
  <foreground android:drawable="@mipmap/ic_launcher_foreground"/>
</adaptive-icon>
```

Create `android/app/src/main/res/mipmap-anydpi-v26/ic_launcher_round.xml`（同内容）。
Create `android/app/src/main/res/values/colors.xml`:

```xml
<resources>
  <color name="ic_launcher_background">#A8E6CF</color>
</resources>
```

前景图 `ic_launcher_foreground.png` 从源图生成（108dp 内居中裁剪 66% 区域，各密度 108/162/216/324/432px）。

- [ ] **Step 3: 构建验证**

Run: `cd android && ./gradlew :app:assembleDebug`（Expected: BUILD SUCCESSFUL）

```bash
git add android/app/src/main/res/
git commit -m "feat: integrate app icon with adaptive icon support"
```

---

### Task 14: 全量验证 + 真机回归

**Files:** None

- [ ] **Step 1: tsc + jest**

Run: `npx tsc --noEmit && npx jest`（Expected: 0 报错 + 全绿）

- [ ] **Step 2: 构建安装真机**

Run: `cd android && ./gradlew app:installDebug -PreactNativeDevServerPort=8081`

- [ ] **Step 3: 真机回归清单**

- [ ] 首页右上角主题切换：浅色→深色→自适应，全页面配色变化
- [ ] 编辑规则：点击条件/动作卡弹 Modal 编辑；动作开关关闭后触发时该动作日志「已停用」
- [ ] 震动：规则配 urgent 模式，模拟触发感受急促震动
- [ ] 日志页：多次模拟后历史日志不丢（>2 条）
- [ ] 日志页「短信记录」Tab：显示全部监听短信 + 命中状态
- [ ] 模拟 Modal：点模板 chip 快速填入
- [ ] 规则长按复制出「副本」
- [ ] 通知点击直达日志页
- [ ] 导出分享 JSON；导入粘贴还原
- [ ] 新图标显示

- [ ] **Step 4: 最终提交**

```bash
git add -A
git status
git commit -m "chore: ux polish validation"
```

---

## 计划自查

**1. Spec 覆盖：**
- 主题三态 + token 化 ✓ T1/T2
- 弹窗编辑 + 动作开关 + 条件删除隐藏 ✓ T3/T4
- 震动 mode/pattern/amplitude + 原生 ✓ T5
- 日志全量修复 ✓ T6
- 图标 mipmap + adaptive ✓ T13
- 模拟模板 ✓ T9；规则复制 ✓ T10；通知直达 ✓ T11；导入导出 ✓ T12；短信记录 ✓ T7/T8

**2. 占位符扫描：** T13 依赖主人素材（已标注「待主人素材」，非 TBD——用默认路径占位可执行）。

**3. 类型一致性：**
- `TriggerAction.enabled?`（T3）↔ ActionEditor Switch（T4）↔ ActionExecutor 跳过（T3）✓
- `SmsRecord`（T7）↔ LogScreen Tab（T8）✓
- `VibrationModule.vibrate(pattern, amplitude)`（T5）↔ JS NativeModules.VibrationModule（T5）✓
- `navigationRef`（T11）↔ App.tsx ref + trigger/index.ts ✓
- theme colors 字段（T1）↔ 各组件 useTheme（T2）✓
